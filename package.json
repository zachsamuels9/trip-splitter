{
  "name": "trip-split",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "description": "Shareable trip expense splitter with receipt OCR and PWA support.",
  "scripts": {
    "dev": "node server.js",
    "lint": "node scripts/lint.mjs",
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
